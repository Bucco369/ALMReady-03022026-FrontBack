# Core
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
pydantic>=2.10,<3
python-multipart>=0.0.18

# Data processing
numpy>=2.2,<3
pandas>=2.2,<3
pyarrow>=18,<20
openpyxl>=3.1,<4
matplotlib>=3.9,<4

# Serialization
orjson>=3.10,<4

# Test dependencies
httpx>=0.27
pytest>=8.0

# Build / packaging (not needed at runtime – used by CI to produce the frozen sidecar)
pyinstaller>=6.0,<7
