# ── Dependencies ──
node_modules/

# ── Build output ──
dist/
dist-ssr/

# ── Logs ──
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── Environment / secrets ──
*.local
.env
.env.*

# ── Editor / IDE ──
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── macOS ──
.DS_Store
**/.DS_Store

# ── Python ──
**/__pycache__/
*.pyc
*.pyo
.pytest_cache/
backend/.venv/

# ── Backend runtime data (sessions, uploads, caches) ──
backend/data/

# ── Test artifacts (generated PNGs, CSVs) ──
backend/almready/tests/out/

# ── Test fixture ZIPs (too large to track) ──
backend/almready/tests/fixtures/positions/*.zip

# ── Claude Code ──
.claude/
